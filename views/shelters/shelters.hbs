<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Nuestras Casas de acogidas</title>
    <style>
        a {
            margin-right: 16px;
        }
    </style>
</head>
<body>
    <nav>
        <a href="/">Ir a Inicio</a>
        {{!-- <a href="/shelters/create-shelter">Crear Casa de acogida</a> --}}
    </nav>

    <h1>Todas nuestras casas de acogida</h1>
    <ul>
        {{#each shelters}}
            <li>
                <a href="/shelters/{{_id}}">
                    <h3>Name {{name}}</h3>
                </a>
                <h3>Location {{location}}</h3>

                {{#each pets}}
                    <div>{{name}} - {{specie}}</div>
                {{/each}}

                <form method="POST" action="/shelters/{{_id}}">
                    <button type="submit">Eliminar</button>
                </form>
            </li>
            <hr />
        {{/each}}
    </ul>
</body>
</html>