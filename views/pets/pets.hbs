<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Nuestras Mascotas</title>
    <style>
        a {
            margin-right: 16px;
        }
    </style>
</head>
<body>
    <nav>
        <a href="/">Ir a Inicio</a>
        <a href="/pets/create">Crear Mascota</a>
    </nav>

    <h1>Todas nuestras mascotas</h1>
    <ul>
        {{#each pets}}
            <li>
                <a href="/pets/{{_id}}">
                    <h3>Name {{name}}</h3>
                </a>
                <h3>Age {{age}}</h3>
                <h3>Color: {{mayus color}}</h3>
                <h3>Specie: {{specie}}</h3>
                <h3>Breed: {{breed}}</h3>
                {{#mayorque age 5}}
                    <p>Esta mascota es mayor de 5 años!</p>
                {{/mayorque}}

                {{#if image}}
                    <img src="{{image}}" alt="avatar">
                {{/if}}

                <form method="POST" action="/pets/{{_id}}">
                    <button type="submit">Eliminar</button>
                </form>
            </li>
            <hr />
        {{/each}}
    </ul>
</body>
</html>